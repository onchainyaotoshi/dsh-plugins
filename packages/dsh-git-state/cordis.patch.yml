# Layer bundle dsh-git-state — diterapkan setelah semua bundle sebelumnya.
# Satu row ini sekaligus: host half (plugin Node: route API) DAN browser roster
# (dsh.client → bundle di-serve di /plugins/dsh-git-state/client.js).
- insert:
    - id: git-state
      name: dsh-git-state
